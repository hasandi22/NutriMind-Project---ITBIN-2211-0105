specVersion: "v1"
serviceId: "nutrimind"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nutrimind-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
